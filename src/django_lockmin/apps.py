from django.apps import AppConfig


class LockminAppConfig(AppConfig):
    """
    Used to provide visual improvements when displaying the dashboards in the Admin view.
    """

    name = "lockmin"
    verbose_name = "Django Lockmin"
